import 'package:business_chat/pages/contact_page.dart';
import 'package:business_chat/pages/home_page.dart';
import 'package:flutter/material.dart';

//class ContactProvider extends ChangeNotifier {
  // final List<Employees> contacts = [];

  // ContactProvider() {
  //   List<String> keyList = m.keys.toList();

  //   for (int i = 0; i < m.length; i++) {
  //     String key = keyList[i];
  //     contacts.add(Employees(name: key, role: m[key] ?? 'N?A'));
  //   }
  // }

  // void notify() {
  //   notifyListeners();
  // }

  // void press(String id) {
  //   for (Employees item in contacts) {
  //     if (item.id == id) {
  //       item.press();
  //       notifyListeners();
  //     }
//}
